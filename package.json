{
  "name": "paper-label-open-source",
  "private": true,
  "version": "0.1.0",
  "license": "MPL-2.0",
  "packageManager": "npm@10.9.3",
  "workspaces": [
    "apps/extension",
    "packages/contracts"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspace @paper-label/contracts && npm run typecheck --workspace @paper-label/extension",
    "test": "npm run test --workspace @paper-label/contracts && npm run test --workspace @paper-label/extension",
    "build": "npm run build --workspace @paper-label/contracts && npm run build --workspace @paper-label/extension",
    "zip": "npm run zip --workspace @paper-label/extension"
  }
}
